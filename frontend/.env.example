# Copy this to .env for local dev.
VITE_API_BASE_URL=http://localhost:8000

# When you deploy the backend to Render, set this same variable in Vercel's
# project settings to your Render URL instead, e.g.
# VITE_API_BASE_URL=https://your-backend.onrender.com
